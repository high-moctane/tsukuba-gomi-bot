#! /bin/sh
exec rbenv exec bundle exec ruby -S -x "$0" "$@"
#! ruby

require "open3"

clockwork_path = File.expand_path("../../lib/clockwork.rb", __FILE__)
out, err, status = Open3.capture3("rbenv exec bundle exec clockwork #{clockwork_path}")

puts out
warn err + "\n"
p status
